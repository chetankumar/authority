# Authority backend — pinned dependencies (locked stack, doc 01 §Tech stack)
# The API is the single writer: FastAPI + one Uvicorn worker.

fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
filelock==3.16.1

# Version control: real git repo per book (requires system git installed).
GitPython==3.1.44

# AI system (doc 05). Not exercised by the skeleton phase, declared here so the
# environment matches the locked stack.
langchain-core==0.3.29
langchain-anthropic==0.3.1
langchain-openai==0.3.0
langchain-ollama==0.2.2
langchain-google-genai==2.0.9
